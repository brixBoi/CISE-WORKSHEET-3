{
  "name": "worksheet-3",
  "version": "1.0.0",
  "description": "CISE REACT Worksheet 3",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "cd frontend && npm run build",
    "install-client": "cd frontend && npm install",
    "heroku-postbuild": "npm run install-client && npm run build",
    "server": "nodemon app.js",
    "develop": "npm run server",
    "start": "npm run server"
  },
  "author": "Zach Martin",
  "license": "ISC",
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "body-parser": "^1.19.0",
    "config": "^3.3.6",
    "cors": "^2.8.5",
    "dotenv": "^10.0.0",
    "express": "^4.17.1",
    "mongoose": "^6.0.5",
    "validate": "^5.1.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.12"
  }
}
